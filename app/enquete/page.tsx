import EnqueteClient from "./EnqueteClient";

export const metadata = {
  title: "L'enquête — wikifela",
  description: "L'enquête : Quiz, Tapissage et GeoFELA en mode Survie.",
};

export default function EnquetePage() {
  return <EnqueteClient />;
}
