import NewQuestionClient from "./NewQuestionClient";

export const metadata = {
  title: "Nouvelle question — Admin wikifela",
  robots: "noindex, nofollow",
};

export default function NewQuestionPage() {
  return <NewQuestionClient />;
}
