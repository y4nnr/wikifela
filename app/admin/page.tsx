import AdminDashboard from "./AdminDashboard";

export const metadata = {
  title: "Admin — wikifela",
  robots: "noindex, nofollow",
};

export default function AdminPage() {
  return <AdminDashboard />;
}
