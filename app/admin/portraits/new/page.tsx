import NewPortraitClient from "./NewPortraitClient";

export const metadata = {
  title: "Nouveau portrait — Admin wikifela",
  robots: "noindex, nofollow",
};

export default function NewPortraitPage() {
  return <NewPortraitClient />;
}
