import AdminPortraits from "./AdminPortraits";

export const metadata = {
  title: "Portraits — Admin wikifela",
  robots: "noindex, nofollow",
};

export default function AdminPortraitsPage() {
  return <AdminPortraits />;
}
